#include"main.h"
/**
 * _abs - computes absolute value of an integer
 * Description: @n takes integer input for function
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
