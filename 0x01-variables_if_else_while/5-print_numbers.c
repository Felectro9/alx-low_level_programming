#include <stdio.h>

/**
 *
 * main -Entry point
 *
 * description - prints numbers between 0 and 10
 *
 * return (0) (always/success)
 */

int main(void)
{
	int n;
	n = 0;
	while(n <= 9)
	{
		printf("%i", n)
		++n;

	}

putchar("int\n");

return (0);
}
